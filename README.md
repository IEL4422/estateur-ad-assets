# Estateur Ad Assets

The three Meta ad creatives (1080x1080 PNG) are hosted at the following public URLs, used as `image_url` in the Estateur Meta ads:

- Quiz CTA (ad_quiz.png): https://files.catbox.moe/v2wogd.png
- Price Anchor (ad_price.png): https://files.catbox.moe/lvmg9f.png
- Why DIY Fails / Trust (ad_trust.png): https://files.catbox.moe/9mv4vh.png

Note: direct base64 commit of the full-resolution PNGs into this repo via the automation tool was not reliable, so a stable public host (catbox.moe) is used for the live image URLs. Byte-for-byte identical to the source files (md5 verified).
